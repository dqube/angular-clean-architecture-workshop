import { Observable } from 'rxjs';

// tslint:disable-next-line:no-empty-interface
export interface IHttp<%= classify(name) %>RepositoryService {
  // doSomething(someInput: string): Observable<any>;
}
