import { CreateAccountAction } from './create-account.action';

describe('CreateAccountAction', () => {
  it('should create an instance', () => {
    expect(new CreateAccountAction()).toBeTruthy();
  });
});